# Scraper Lambda dependencies. boto3/botocore provided by Lambda runtime.
# No ddgs/lxml: built on Windows would break on Lambda (lxml is binary). We use HTML-based DuckDuckGo search instead.
requests>=2.31.0
beautifulsoup4>=4.12.0
